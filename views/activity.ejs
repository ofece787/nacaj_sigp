<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="css/reset.css">
  <link rel="stylesheet" href="css/style.css">
  <link rel="stylesheet" href="css/activities.css">
  <link rel="stylesheet" href="css/navigation.css">
  <link rel="stylesheet" href="css/gridBodyStyle.css">
</head>
<body>
  <header>
    <a href="/activitydisplay" class="back-button">
      <img src="../icones/back-arrow.png" alt="">
    </a>
    <div class="app-logo">
      <img src="../icones/logoNACAJalinhado.png" alt="">
      <h1>NACAJ</h1>
    </div>
  </header>
  <main>
    <section class="main-padding signup">
      <div class="form">
        <h1>Activity</h1>
        <form action="">
          <div class="input-div">
            <div class="input title-div">
              <label for="activity-title">Title</label>
              <input type="text" name="title" class="title" id="activity-title" required>
              <div class="activity error"></div>
            </div>
            <div class="input description-div">
              <label for="activity-description">Description</label>
              <textarea name="description" class="description" id="activity-description"></textarea>
              <div class="description error"></div>
            </div>
            <div class="input file">
              <label for="news-image">Escolha uma imagem</label>
              <input type="file" multiple name="images" class="image" id="news-image">
            </div>
          </div>
          <div class="success">

          </div>
          <div class="form-buttons">
            <button>Publicar</button>
            <div class="login-button">Cancelar</div>
          </div>
        </form>
      </div>
    </section>
    
  </main>

  <footer>

    <nav class="navigation">
      <a href="/"><img src="../icones/world_2298220.png" alt=""></a>
      <a href="/activitydisplay"><img src="../icones/checklist_7111148.png" alt=""></a>
    </nav>
  </footer>



  <script>
    const formSignup = document.querySelector('form')
    const titleError = document.querySelector('.title.error')
    const descriptionError = document.querySelector('.description.error')
    const title = document.querySelector('.title')
    const description = document.querySelector('.description')
    const fileInput = document.querySelector('.image')
    const successMessage = document.querySelector('.success')
    
    formSignup.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const files = fileInput.files
      console.log(files)
      const formData = new FormData();
      formData.append('title', title.value);
      formData.append('description', description.value);
      Array.from(files).forEach(file => formData.append('images', file))
      try {
        const res = await fetch('/activity', {
          method: 'POST',
          body: formData
        })
        const data = await res.json()
        console.log(data)
        if(data.err) {
          titleError.textContent = data.err.email
          descriptionError.textContent = data.err.password
        } else{
          successMessage.textContent = data.message
        }
      } catch (error) {
        console.log(error)
      }
    })
  </script>

</body>
</html>